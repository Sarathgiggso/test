apiVersion: v1
kind: Service
metadata:
  labels:
    service: bert-encoder
  name: bert-encoder
spec:
  type: ClusterIP
  ports:
  - name: "8190"
    port: 8080
    targetPort: 8080
  selector:
    service: bert-encoder

